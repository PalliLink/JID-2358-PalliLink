import 'package:flutter/material.dart';

class FacilitiesPage extends StatelessWidget {
  const FacilitiesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Associated Facilities'),
      ),
      body: ListView(
        children: [],
      ),
    );
  }
}
